#include <stdio.h>

int main (){
	int numeros[10], i;
	printf("Digite 10 valores inteiros: \n");
	
	for(i=0; i<10; i++)
		scanf("%d", &numeros[i]);
		
		for (i=0; i<10; i++)
		printf("%d ", numeros[i]);
		return 0;
	
		}
