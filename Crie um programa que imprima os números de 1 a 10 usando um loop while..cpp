#include <stdio.h>

int main() {
    int numero = 1;

    // Loop while para imprimir os números de 1 a 10
    while (numero <= 10) {
        printf("%d\n", numero);
        numero++;
    }

    return 0;
}

